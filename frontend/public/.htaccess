<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Redirect sitemap.xml to the dynamic backend sitemap
  RewriteRule ^sitemap\.xml$ https://zonirazjewellery.onrender.com/sitemap.xml [R=301,L]

  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-l
  RewriteRule . /index.html [L]
</IfModule>
